// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/grokify/omniroadmap/ent/syncmeta"
)

// SyncMeta is the model entity for the SyncMeta schema.
type SyncMeta struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// Provider holds the value of the "provider" field.
	Provider string `json:"provider,omitempty"`
	// Kind holds the value of the "kind" field.
	Kind string `json:"kind,omitempty"`
	// LastSync holds the value of the "last_sync" field.
	LastSync time.Time `json:"last_sync,omitempty"`
	// RecordCount holds the value of the "record_count" field.
	RecordCount  int `json:"record_count,omitempty"`
	selectValues sql.SelectValues
}

// scanValues returns the types for scanning values from sql.Rows.
func (*SyncMeta) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case syncmeta.FieldID, syncmeta.FieldRecordCount:
			values[i] = new(sql.NullInt64)
		case syncmeta.FieldProvider, syncmeta.FieldKind:
			values[i] = new(sql.NullString)
		case syncmeta.FieldLastSync:
			values[i] = new(sql.NullTime)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the SyncMeta fields.
func (_m *SyncMeta) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case syncmeta.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			_m.ID = int(value.Int64)
		case syncmeta.FieldProvider:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field provider", values[i])
			} else if value.Valid {
				_m.Provider = value.String
			}
		case syncmeta.FieldKind:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field kind", values[i])
			} else if value.Valid {
				_m.Kind = value.String
			}
		case syncmeta.FieldLastSync:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field last_sync", values[i])
			} else if value.Valid {
				_m.LastSync = value.Time
			}
		case syncmeta.FieldRecordCount:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field record_count", values[i])
			} else if value.Valid {
				_m.RecordCount = int(value.Int64)
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the SyncMeta.
// This includes values selected through modifiers, order, etc.
func (_m *SyncMeta) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// Update returns a builder for updating this SyncMeta.
// Note that you need to call SyncMeta.Unwrap() before calling this method if this SyncMeta
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *SyncMeta) Update() *SyncMetaUpdateOne {
	return NewSyncMetaClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the SyncMeta entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *SyncMeta) Unwrap() *SyncMeta {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("ent: SyncMeta is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *SyncMeta) String() string {
	var builder strings.Builder
	builder.WriteString("SyncMeta(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	builder.WriteString("provider=")
	builder.WriteString(_m.Provider)
	builder.WriteString(", ")
	builder.WriteString("kind=")
	builder.WriteString(_m.Kind)
	builder.WriteString(", ")
	builder.WriteString("last_sync=")
	builder.WriteString(_m.LastSync.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("record_count=")
	builder.WriteString(fmt.Sprintf("%v", _m.RecordCount))
	builder.WriteByte(')')
	return builder.String()
}

// SyncMetaSlice is a parsable slice of SyncMeta.
type SyncMetaSlice []*SyncMeta
