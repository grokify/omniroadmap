// Code generated by ent, DO NOT EDIT.

package ent

import (
	"encoding/json"
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/grokify/omniroadmap/ent/opportunityspec"
	"github.com/grokify/prism-roadmap/canvas"
)

// OpportunitySpec is the model entity for the OpportunitySpec schema.
type OpportunitySpec struct {
	config `json:"-"`
	// ID of the ent.
	ID string `json:"id,omitempty"`
	// Title holds the value of the "title" field.
	Title string `json:"title,omitempty"`
	// CreatedAt holds the value of the "created_at" field.
	CreatedAt time.Time `json:"created_at,omitempty"`
	// UpdatedAt holds the value of the "updated_at" field.
	UpdatedAt time.Time `json:"updated_at,omitempty"`
	// Canonical holds the value of the "canonical" field.
	Canonical    canvas.OpportunitySpec `json:"canonical,omitempty"`
	selectValues sql.SelectValues
}

// scanValues returns the types for scanning values from sql.Rows.
func (*OpportunitySpec) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case opportunityspec.FieldCanonical:
			values[i] = new([]byte)
		case opportunityspec.FieldID, opportunityspec.FieldTitle:
			values[i] = new(sql.NullString)
		case opportunityspec.FieldCreatedAt, opportunityspec.FieldUpdatedAt:
			values[i] = new(sql.NullTime)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the OpportunitySpec fields.
func (_m *OpportunitySpec) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case opportunityspec.FieldID:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value.Valid {
				_m.ID = value.String
			}
		case opportunityspec.FieldTitle:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field title", values[i])
			} else if value.Valid {
				_m.Title = value.String
			}
		case opportunityspec.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				_m.CreatedAt = value.Time
			}
		case opportunityspec.FieldUpdatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field updated_at", values[i])
			} else if value.Valid {
				_m.UpdatedAt = value.Time
			}
		case opportunityspec.FieldCanonical:
			if value, ok := values[i].(*[]byte); !ok {
				return fmt.Errorf("unexpected type %T for field canonical", values[i])
			} else if value != nil && len(*value) > 0 {
				if err := json.Unmarshal(*value, &_m.Canonical); err != nil {
					return fmt.Errorf("unmarshal field canonical: %w", err)
				}
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the OpportunitySpec.
// This includes values selected through modifiers, order, etc.
func (_m *OpportunitySpec) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// Update returns a builder for updating this OpportunitySpec.
// Note that you need to call OpportunitySpec.Unwrap() before calling this method if this OpportunitySpec
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *OpportunitySpec) Update() *OpportunitySpecUpdateOne {
	return NewOpportunitySpecClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the OpportunitySpec entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *OpportunitySpec) Unwrap() *OpportunitySpec {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("ent: OpportunitySpec is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *OpportunitySpec) String() string {
	var builder strings.Builder
	builder.WriteString("OpportunitySpec(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	builder.WriteString("title=")
	builder.WriteString(_m.Title)
	builder.WriteString(", ")
	builder.WriteString("created_at=")
	builder.WriteString(_m.CreatedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("updated_at=")
	builder.WriteString(_m.UpdatedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("canonical=")
	builder.WriteString(fmt.Sprintf("%v", _m.Canonical))
	builder.WriteByte(')')
	return builder.String()
}

// OpportunitySpecs is a parsable slice of OpportunitySpec.
type OpportunitySpecs []*OpportunitySpec
